{
  "name": "zora-test01",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ zora-test01",
    "create-local": "graph create --node http://localhost:8020/ zora-test01",
    "remove-local": "graph remove --node http://localhost:8020/ zora-test01",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zora-test01"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
